#Builds the docker image
docker build -t ltt_backpack:ltt_backpack /mnt/user/Documents/ltt_backpack

#Removes the docker image
docker rmi -f df455cd7a122

#Runs the image in a docker container, removes the log file, and removes the 
#container (Unraid)
#!/bin/bash
: > $(docker inspect --format='{{.LogPath}}' df455cd7a122)
docker run --name ltt_backpack -v /mnt/user/Documents/ltt_backpack:/app df455cd7a122
docker rm ltt_backpack